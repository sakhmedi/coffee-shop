/**
 * Точки на карте. Здесь только координаты и ключ в словаре: название, адрес
 * и часы работы переводятся, поэтому живут в locale.js под addresses.<id>,
 * а не рядом с широтой и долготой.
 *
 * Порядок = порядок карточек под картой.
 */

/** @type {{ id: string, coords: [number, number] }[]} */
export const places = [
  { id: 'nurAlem', coords: [51.0906103, 71.424228] },
  // Координаты с карточки 2ГИС по адресу «Проспект Женис, 1», Сарыарқа район.
  { id: 'centralPark', coords: [51.15995, 71.415257] },
];
