/**
 * Заголовок первого экрана латиницей — казахский на латинской графике.
 *
 * Почему не в locale.js: это не перевод. Тот же казахский текст, что лежит
 * под ключом hero.title, записанный другой графикой и существующий только
 * как визуальный слой — скринридер читает кириллический hero.title, а эту
 * строку закрывает aria-hidden. В словаре она выглядела бы третьим языком
 * и попала бы в t(), откуда её мог бы прочитать кто угодно.
 *
 * Строка держится на Unbounded (--font-wide). Менять её можно только вместе
 * с проверкой cmap: ǵ (U+01F5) лежит в latin-ext, и любой новый символ надо
 * проверить так же — иначе он молча уйдёт в фолбэк, ровно как это случилось
 * с ғ в кириллическом заголовке. Команды проверки — в CLAUDE.md.
 */
export const HERO_TITLE_LATIN = 'Dala ortasyndaǵy kofe';

/** Язык, в котором заголовок первого экрана показываем латиницей. */
export const LATIN_LANG = 'kk';
